# õzger - Полная инструкция по запуску

## Обзор проекта

õzger - это образовательная платформа для подготовки к ЕНТ с интегрированным AI Teacher.

### Структура проекта

```
ozger/
├── frontend/           # Frontend (HTML, CSS, JS)
│   ├── index.html
│   ├── script.js
│   ├── styles.css
│   └── fonts/
├── backend/
│   ├── server.js       # Node.js сервер (основной)
│   ├── package.json
│   └── ai-teacher-api/ # Python AI сервер
│       ├── app.py
│       ├── requirements.txt
│       └── services/
│           ├── gemini_service.py
│           └── pdf_service.py
└── README.md
```

---

## Требования

### 1. Node.js (для основного бэкенда)
- Node.js 18+ 
- npm

### 2. Python (для AI Teacher API)
- Python 3.9+
- pip

### 3. API ключи
- **Google Gemini API Key** - получить на https://makersuite.google.com/app/apikey
- **Supabase** (если используете аутентификацию) - настроен в проекте

---

## Шаг 1: Настройка AI Teacher API (Python)

### 1.1 Перейдите в папку AI Teacher

```bash
cd ozger/backend/ai-teacher-api
```

### 1.2 Создайте виртуальное окружение

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 1.3 Установите зависимости

```bash
pip install -r requirements.txt
```

### 1.4 Создайте файл .env

Создайте файл `.env` в папке `ai-teacher-api`:

```env
# Google Gemini API Key (ОБЯЗАТЕЛЬНО!)
GEMINI_API_KEY=ваш_ключ_gemini_api

# Порт сервера (по умолчанию 5000)
AI_TEACHER_PORT=5000

# Режим отладки
FLASK_DEBUG=True
```

### 1.5 Запустите AI Teacher API

```bash
python app.py
```

Вы должны увидеть:
```
🚀 AI Teacher API starting on port 5000
📚 Ready to help with ENT preparation!
```

---

## Шаг 2: Настройка основного бэкенда (Node.js)

### 2.1 Перейдите в папку backend

```bash
cd ozger/backend
```

### 2.2 Установите зависимости

```bash
npm install
```

### 2.3 Настройте переменные окружения

Создайте файл `.env` в папке `backend`:

```env
# Server Configuration
PORT=3001
NODE_ENV=development

# Supabase Configuration (ОБЯЗАТЕЛЬНО!)
# Получите ключи на https://supabase.com/dashboard → Project Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_key
 
# Session
SESSION_SECRET=your-super-secure-session-secret-at-least-32-chars

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

**ВАЖНО:** Без правильных Supabase ключей авторизация и сохранение тестов работать не будут!
Если вы видите ошибку "Supabase not configured", проверьте переменные окружения.

### 2.4 Запустите Node.js сервер

```bash
npm start
```

Или для разработки:
```bash
npm run dev
```

---

## Шаг 3: Запуск Frontend

### Вариант A: Использование Live Server (рекомендуется для разработки)

1. Откройте папку `frontend` в VS Code
2. Установите расширение "Live Server"
3. Правый клик на `index.html` → "Open with Live Server"

### Вариант B: Простой HTTP сервер

```bash
cd ozger/frontend

# Python 3
python -m http.server 8080

# Node.js (если установлен http-server)
npx http-server -p 8080
```

Затем откройте: http://localhost:8080

---

## Полный запуск (все сервисы)

### Терминал 1: AI Teacher API
```bash
cd ozger/backend/ai-teacher-api
venv\Scripts\activate  # Windows
python app.py
```

### Терминал 2: Node.js Backend
```bash
cd ozger/backend
npm start
```

### Терминал 3: Frontend (опционально)
```bash
cd ozger/frontend
python -m http.server 8080
```

---

## Использование AI Teacher

После запуска всех сервисов:

1. Откройте http://localhost:8080 (или порт Live Server)
2. На главной странице нажмите одну из кнопок:
   - **📚 Үйрену (Learn)** - Режим обучения с планом и вопросами
   - **✏️ Жаттығу (Practice)** - Практика с флеш-картами и тестом
   - **🎯 Real Test** - Тест в формате ЕНТ

3. Выберите тип материала:
   - **📝 Мәтін** - Вставьте текст вручную
   - **📄 PDF** - Загрузите PDF файл

4. Для режима Learn:
   - Можете включить **History Mode** для тарихи материалов (хронология, конспект)
   
5. Для Practice/Real Test:
   - Выберите количество вопросов (10, 15, 20, 25, 30)

---

## Устранение неполадок

### AI Teacher API не работает

1. Проверьте, что Python venv активирован
2. Убедитесь, что GEMINI_API_KEY задан в .env
3. Проверьте порт 5000 свободен

```bash
# Windows - проверить порт
netstat -an | findstr "5000"
```

### Ошибка CORS

Убедитесь, что оба сервера запущены и используют правильные порты:
- AI Teacher API: http://localhost:5000
- Node.js: http://localhost:3001

### Ошибка генерации

1. Проверьте API ключ Gemini
2. Убедитесь, что материал не слишком длинный (макс ~15000 символов)
3. Проверьте интернет-соединение

### Supabase not configured

Если видите эту ошибку в консоли:

1. Создайте файл `.env` в папке `backend`
2. Добавьте корректные значения `SUPABASE_URL` и `SUPABASE_ANON_KEY`
3. Получите ключи на https://supabase.com/dashboard → ваш проект → Settings → API
4. Перезапустите Node.js сервер

### npm audit предупреждения

При установке npm может показать предупреждения о уязвимостях. 
Выполните `npm audit fix` для исправления. Оставшиеся предупреждения 
(low severity) связаны с устаревшим пакетом `csurf` и безопасны для использования.

---

## API Endpoints (AI Teacher)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/health` | Проверка работоспособности |
| POST | `/api/upload` | Загрузка материала (text/PDF) |
| POST | `/api/generate/learn` | Генерация плана обучения |
| POST | `/api/generate/practice` | Генерация практики |
| POST | `/api/generate/realtest` | Генерация теста ЕНТ |
| POST | `/api/generate/continue` | Продолжение с новыми вопросами |

---

## Поддержка

При возникновении проблем проверьте:
1. Все сервисы запущены
2. API ключи настроены правильно
3. Порты не заняты другими приложениями
